# FROM - які корневі програми потрібні для запуску 
# WORKDIR - створити робочу директорію (папку з проектом) 
# COPY - скопіювати файли проекту в папку (вказати звіди і куди) ("." - з корневого каталогу в "." - робочу директорію) 
# RUN - встановити пакети з package.json 
# EXPOSE - номер порта 
# CMD - задати команду для запуску проекту (START = "node app") 

FROM node 
WORKDIR /app 
COPY . . 
RUN npm install 
EXPOSE 3000 
CMD [ "node", "server" ]
